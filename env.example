TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Optional — BSC JSON-RPC (countdown + on-chain prediction bets)
# BSC_RPC_URL=https://bsc-dataseed.binance.org

# Optional — BSC_WALLET_PRIVATE_KEY enables on-chain taps.
# Real strategy + auto-placement stake is configured in `src/config.ts` (default 0.05 BNB).
# /fakesignal and /placement up|down: fixed 0.0015 BNB.
# Use a HOT wallet with limited funds only.
# BSC_WALLET_PRIVATE_KEY=
