TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Optional — BSC JSON-RPC (countdown + on-chain prediction bets)
# BSC_RPC_URL=https://bsc-dataseed.binance.org

# Optional — BSC_WALLET_PRIVATE_KEY also enables /placement up|down (fixed 0.003 BNB test stake).
# With PANCAKE_PREDICTION_BET_BNB, tapping UP/DOWN on pre-prediction sends betBull/betBear (that amount).
# Use a HOT wallet with limited funds only.
# BSC_WALLET_PRIVATE_KEY=
# PANCAKE_PREDICTION_BET_BNB=0.01
